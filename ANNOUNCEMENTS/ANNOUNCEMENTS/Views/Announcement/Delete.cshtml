@model Repository.Models.Announcement

@{
    ViewBag.Title = "Delete Announcement number " + Model.Id;
    ViewBag.Description = "Delete Announcement number " + Model.Id + "Description for Google";
    ViewBag.KeyWords = "Announcement, " + Model.Id + ", delete";
}

<h2>Delete Announcement number: @Model.Id</h2>

@if (ViewBag.Error == true)
{
    <div class="alert alert-danger" role="alert">
        Appear the error during delete.
        <br />
        Once Again.
    </div>
}


<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Announcement</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            User Id:
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Content)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Content)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DateOfAdd)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DateOfAdd)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger confirmation" /><!--onclick="return confirm('Are You sure to delete this announcement?')"/>--> 
            @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-warning"})
        </div>
    }

   @section Scripts{
       <script type="text/javascript">
           $('.confirmation').on('click', function () {
               return confirm('Are You sure to delete this announcement?');
           });
       </script>
       } 
</div>
